$kind: http-request
url: "{{url}}/sprint-service/api/v1/task-sprints"
method: POST
body:
  type: json
  content: |-
    {
        "taskId": "{{taskId}}",
        "sprintId": "{{sprintId}}"
    }
order: 5000
