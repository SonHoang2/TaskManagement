$kind: http-request
url: "{{url}}/task-service/api/v1/task-comments"
method: POST
body:
  type: json
  content: |-
    {
      "taskId": "{{taskId}}",
      "userId": "{{userId}}",
      "content": "This is a sample comment for the task."
    }
order: 1000
