$kind: http-request
url: "{{url}}/notification-service/api/v1/notifications/:id"
method: PATCH
pathVariables:
  id: "{{notificationId}}"
body:
  type: json
  content: |-
    {
      "isRead": true
    }
order: 2000
