$kind: http-request
url: "{{url}}/project-service/projects/invites/:id"
method: PATCH
pathVariables:
  id: "{{invitationId}}"
body:
  type: json
  content: |-
    {
        "decision" : "ACCEPT"
    }
order: 6000
