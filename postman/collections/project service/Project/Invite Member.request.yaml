$kind: http-request
url: "{{url}}/project-service/projects/:id/invites"
method: POST
pathVariables:
  id: "{{projectId}}"
body:
  type: json
  content: |-
    {
        "userId" : "{{invitedUserId}}"
    }
order: 5000
