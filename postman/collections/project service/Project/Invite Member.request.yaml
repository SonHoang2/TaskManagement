$kind: http-request
url: "{{url}}/project-service/api/v1/projects/:id/invites"
method: POST
pathVariables:
  id: "{{projectId}}"
body:
  type: json
  content: |-
    {
        "userId" : "{{invitedUserId}}"
    }
order: 5000
