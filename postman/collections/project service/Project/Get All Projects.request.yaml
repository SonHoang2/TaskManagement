$kind: http-request
name: Get All Projects
url: "{{url}}/project-service/api/v1/projects?size=1&sortBy=membercount&sortDirection=desc&search=beacon delta haven prism"
method: GET
queryParams:
  - key: size
    value: "1"
    disabled: false
    description: ""
  - key: sortBy
    value: membercount
    disabled: false
    description: ""
  - key: sortDirection
    value: desc
    disabled: false
    description: ""
  - key: search
    value: beacon delta haven prism
    disabled: false
    description: ""
scripts:
  - type: afterResponse
    code: "// 1. Parse dữ liệu JSON từ response trả về\r

      const responseData = pm.response.json();\r

      \r

      // 2. Truy cập vào phần tử đầu tiên trong mảng \"projects\" và lấy \"id\"\r

      const firstProjectId = responseData.data.content[0].id;\r

      \r

      // 3. Lưu project id vào biến môi trường (Environment Variable) của Postman\r

      pm.environment.set(\"projectId\", firstProjectId);"
    language: text/javascript
order: 2000
